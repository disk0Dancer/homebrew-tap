# homebrew-tap

Homebrew tap for disk0Dancer CLI formulas.

## Install climate

```bash
brew tap disk0Dancer/tap
brew install climate
```

You can also install directly:

```bash
brew install disk0Dancer/tap/climate
```
